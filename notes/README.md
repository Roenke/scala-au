Simple web-application written in play+scalajs to store notes.

Dependencies: `sbt`

To start use the following commands
```$bash
$ sbt
> run
```

Then the application available on 9000 (by default).
